{
  "name": "retro-portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "node backend/index.js",
    "build": "build": "npm install --prefix backend && npm install --prefix frontend && cd frontend && npm run build && cd .. && mkdir -p backend/public && cp -r frontend/dist/* backend/public/",
    "postinstall": "npm install --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Pawketz/retro-portfolio.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Pawketz/retro-portfolio/issues"
  },
  "homepage": "https://github.com/Pawketz/retro-portfolio#readme",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mongoose": "^8.17.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10",
    "concurrently": "^9.2.0"
  }
}